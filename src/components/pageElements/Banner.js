import React from "react";

const Banner = () => {
  return (
    <div
      className="absolute -z-50 w-full h-full bg-cover md:bg-top h-[80vh] md:bg-top left-0 top-0 lg:bg-right-top"
      id="homeBanner"
    ></div>
  );
};

export default Banner;
